{
  "name": "@big-rocks/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP adapter: exposes the shared @big-rocks/core service layer as MCP tools over stdio. No business logic of its own — a clearly separable module with its own entry point, extractable later by pointing it at the REST API instead.",
  "bin": {
    "big-rocks-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@big-rocks/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
